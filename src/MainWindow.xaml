<Window x:Class="DirSizeViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="DirSizeViewer" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button x:Name="SelectFolderButton" Grid.Row="0" Content="フォルダを選択..." 
                HorizontalAlignment="Left" VerticalAlignment="Top" 
                Margin="10" Padding="10,5"
                Click="SelectFolderButton_Click"/>

        <DataGrid x:Name="FilesDataGrid" Grid.Row="1" 
                  Margin="10,0,10,10"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="名前" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="サイズ" Binding="{Binding SizeString}" SortMemberPath="Size" Width="120">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextAlignment" Value="Right"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="種類" Binding="{Binding Type}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
